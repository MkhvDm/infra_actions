name: DjangoApp workflow

on: 
  push:
    branches: [ main ]
  # pull_request:
  #   branches: [ main ]

jobs:
  build: 
    runs-on:
      ubuntu-latest

    steps:
    - name: Test GitHUb action command
      run: echo github action ok!
